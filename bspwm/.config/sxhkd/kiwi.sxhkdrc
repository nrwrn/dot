alt + {_, shift + }{u,i,o,p,comma,period,y}
	bspc {desktop -f,node -d} '{www,code,discord,games,book,six,music}'

super + {_, shift + }{1,2,3,4,5,6,7,8,9,0}
	bspc {desktop -f,node -d} '{www,code,discord,games,book,six,seven,music,nine,ten}'

Pause
	mkdir -p ~/Pictures/scrot && \
	scrot -u ~/Pictures/scrot/$(uname -n).png && \
	notify-send -t 3000 "took scrot"
