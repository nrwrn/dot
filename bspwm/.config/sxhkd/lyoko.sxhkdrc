alt + {_, shift + }{u,i,o,p,comma,period,y}
	bspc {desktop -f,node -d} '{left,steam,right,discord,code,docs,tv}'

super + {_, shift + }{1,2,3,4,5,6,7}
	bspc {desktop -f,node -d} '{left,steam,right,discord,code,docs,tv}'

Pause
	mkdir -p ~/Pictures/scrot && \
	scrot -u ~/Pictures/scrot/$(uname -n).png && \
	notify-send -t 3000 "took scrot"

